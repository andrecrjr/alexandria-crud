import Layout from "@/components/Layout";
import FormInput from "@/components/forms";

const CreatePage = () => {
  return (    <Layout>
      <FormInput />
    </Layout>)
};

export default CreatePage